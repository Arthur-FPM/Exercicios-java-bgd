let base = prompt("digite o valor da base do quadrado:")
let altura = prompt("digite o valor da altura do quadrado:")
if(base===altura){alert("Isso é um quadrado!")}
else{
let area = base * altura
alert (`A área do retangulo é de ${area}`)}
